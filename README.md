WS1.1 วิธีการทำงานคือสร้างตัวแปรมาเก็บข้อมูล ใช้ FirstName,Age,Branch มาเป็นตัวแปลเก็บข้อมูล
      และและใช้ console.log เพื่อแสดงข้อมูล ใช้คำสั่ง node และก็ชื่อไฟล์ เช่น node helloworld เพื่อรั่นคำสั่ง

      
WS1.2 กำหนด 5 ตัวแปร มาเก็บข้อมูลต่างกัน มี String Number Boolaen Null Undefined 
      และใช้ Typeof เพื่อตรวจสอบ ชนิดของแต่ละตัวแปร และ แสดงออก ทาง console

      
WS1.3 กำหนดตัวแปร คะแนน คณิตศาสตร์ วิทยาศาสตร์ อังกฤษ และ ใช้ ตัวแปร x เพื่อคำนวน นำ ทั้ง 3วิชามารวมกัน
      มี คำนวนคะแนนรวม คำนวนค่าเฉลี่ย และผลคะแนนเฉลี่ย ได้มากว่า 80 หรือไม่ 
      โดย x = คณิตศาสตร์+วิทยาศาสตร์+อังกฤษ 
      x / 3 
      x >=80 

      
WS1.4 กำหนดตัวแปรเก็บ username  password age และ ใช้ if ในการกำหนด คำสั่งโดย ถ้า username  password age ถ้าถูกต้องจะแสดง เข้าสู่ระบบสำเร็จ
      แต่ถ้า อายุไม่เกิน 18 ปี จะแสดง อายุไม่ถึงเกณฑ์ และถ้าชื่อหรือรหัสผ่านผิด จะแสดง ชื่อผู้ใช้หือรหัสผ่านไม่ถูกต้อง


W5 
1. GETคืนค่ารายชื่อนักเรียนทั้งหมดการทำงานส่งข้อมูล array ของนักเรียนทั้งหมดกลับไป
2. GETคืนค่า ข้อมูลนักเรียนตาม ID ที่ระบุการทำงานรับ ID จาก URL parameterค้นหานักเรียนที่มี ID ตรงกันถ้าเจอส่งข้อมูลกลับไป ไม่เจอส่ง 404
3. POSTเพิ่มนักเรียนใหม่การทำงานรับข้อมูล name และ age จาก request bodyสร้าง ID ใหม่โดยเอา ID สูงสุด + 1เพิ่มนักเรียนใหม่เข้า arrayส่งข้อมูลนักเรียนที่สร้างกลับไป (status 201)
4. PUTแก้ไขข้อมูลนักเรียนการทำงานผ่าน middleware validateStudent เช็คข้อมูลก่อนค้นหานักเรียนจาก IDอัพเดต name และ ageส่งข้อมูลที่แก้ไขกลับไป
5. DELETEลบนักเรียนการทำงานค้นหา index ของนักเรียนจาก IDลบออกจาก array ด้วย spliceส่งข้อความยืนยันการลบ

W7 
GET /users/email/:email
รับ email จาก URL ไปหา User ในฐานข้อมูล
ไม่เจอ → 404 | เจอ → ส่งข้อมูล User พร้อมโพสต์ทั้งหมด

DELETE /users/:id
รับ userId มา
ลบโพสต์ของ User นั้นทั้งหมดก่อน แล้วค่อยลบ User

POST /posts
รับ title, content, authorId
สร้างโพสต์ใหม่ (postId สุ่มให้, published = false)

GET /posts
ดึงโพสต์ทั้งหมด พร้อมข้อมูล User เจ้าของโพสต์
ส่งกลับเป็น Array

GET /posts/:id
รับ postId ไปหาโพสต์
ไม่เจอ → 404 | เจอ → ส่งโพสต์พร้อมข้อมูล User

PUT /posts/:id
รับ postId และข้อมูลที่จะแก้
อัปเดตโพสต์ และอัปเดตเวลา updatedAt อัตโนมัติ

DELETE /posts/:id
รับ postId แล้วลบโพสต์
ส่งข้อความยืนยันการลบ